{{ if lookPath "opencode" -}}
#!/usr/bin/env bash
{{ template "helpers.sh" . }}

section "Upgrades"

progress "OpenCode"
if opencode upgrade >/dev/null 2>&1; then
    finish
else
    failed
fi
{{ end -}}
